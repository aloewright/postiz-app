interface:
  display_name: "Postiz App"
  short_description: "Repo-specific patterns and workflows for postiz-app"
  default_prompt: "Use the postiz-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true